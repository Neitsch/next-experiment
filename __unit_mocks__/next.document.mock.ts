import React, { Component } from "react";

export const Head = ({ children }) => children;
export const Main = ({ children }) => children;
export const NextScript = ({ children }) => children;
export default Component;
